"use strict"

 function filterById(array, idToFilter) {
    return array.filter(KINDS => kind_id === idToFilter);
  }

  /*KINDS.values = ;*/
  var KINDS = document.getElementById("Slippers").value.indexOf("1");
  const number = filterById(array, 1);
  console.log(KINDS);